./8-24 | wc -l#include "holberton.h"
int h1, h2, m1, m2, i, n;

void jack_bauer(void)
{
  for (h1 = 0; h1 < 2; h1++)
    {
      for ( h2 = 0; h2 < ; h2++)
	{
	  n = h2 % 10;
	  for (m1 = 48; m1 <= 53; m1++)
	    {
	      for(m2 = 48 ; m2 <= 57; m2++)
		{
		  if( h1 == 2 && h2 == 4)
		    {
		      break;
		    }
		  _putchar(i);
		  _putchar(n);
		  _putchar(':');
		  _putchar(m1);
		  _putchar(m2);
		  _putchar('\n');
		}}}}}

