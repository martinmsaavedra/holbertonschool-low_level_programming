#include "holberton.h"
/**
 * add - add to integers
 * @a: int to add
 * @b: int to add
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int a, int b)
{
	return (a + b);
}
